@page "/Article/{id:int}"
@using ScarNet.Services
@inject ArticleService article
@model ScarNet.Pages.ArticleModel
@{
    var thisArticle = article.GetArticleById(Model.Id);
    ViewData["Title"] = thisArticle.Title;
}
<h1>@ViewData["Title"]</h1>

@{
    this.WriteLiteral(thisArticle.Text);
}